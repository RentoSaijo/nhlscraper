# Build a Hugging Face NHL_DB parquet URL

`.nhldb_url()` builds URLs for the season-level parquet snapshots used
by the bulk loaders.

## Usage

``` r
.nhldb_url(path)
```

## Arguments

- path:

  character path inside the NHL_DB repository

## Value

character scalar URL
