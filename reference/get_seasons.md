# Access all the seasons

`get_seasons()` is deprecated. Use
[`seasons()`](https://rentosaijo.github.io/nhlscraper/reference/seasons.md)
instead.

## Usage

``` r
get_seasons()
```

## Value

data.frame with one row per season
