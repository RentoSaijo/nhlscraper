# Get the three stars of an ESPN event

`get_espn_event_stars()` is defunct. Use
[`gc_summary()`](https://rentosaijo.github.io/nhlscraper/reference/gc_summary.md)
instead.

## Usage

``` r
get_espn_event_stars(event = 401687600)
```
