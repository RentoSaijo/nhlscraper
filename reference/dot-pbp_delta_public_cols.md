# List public delta column names

`.pbp_delta_public_cols()` returns the public-facing delta columns
inserted by
[`add_deltas()`](https://rentosaijo.github.io/nhlscraper/reference/add_deltas.md).

## Usage

``` r
.pbp_delta_public_cols()
```

## Value

character vector of public delta column names
