# Convert a clock duration to seconds

`.html_time_to_seconds()` converts `MM:SS` or `HH:MM:SS` strings to
seconds.

## Usage

``` r
.html_time_to_seconds(x)
```

## Arguments

- x:

  character scalar

## Value

integer scalar or `NA_integer_`
