# Access all the streams

`get_streams()` is deprecated. Use
[`streams()`](https://rentosaijo.github.io/nhlscraper/reference/streams.md)
instead.

## Usage

``` r
get_streams()
```

## Value

data.frame with one row per stream
