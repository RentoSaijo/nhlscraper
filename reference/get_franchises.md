# Access all the franchises

`get_franchises()` is deprecated. Use
[`franchises()`](https://rentosaijo.github.io/nhlscraper/reference/franchises.md)
instead.

## Usage

``` r
get_franchises()
```

## Value

data.frame with one row per franchise
