# Normalize roster person labels

`.normalize_roster_person()` standardizes roster names for stable
matching by uppercasing, trimming, collapsing whitespace, and
transliterating accents.

## Usage

``` r
.normalize_roster_person(x)
```

## Arguments

- x:

  character vector of player names

## Value

normalized character vector
