# Build an HTML report request

`.html_report_request()` constructs an HTML report request with the same
retry policy used for API calls.

## Usage

``` r
.html_report_request(url)
```

## Arguments

- url:

  character scalar

## Value

httr2 request object
