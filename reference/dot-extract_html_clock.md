# Extract an HTML clock token

`.extract_html_clock()` extracts the first `MM:SS`-style clock from a
report cell.

## Usage

``` r
.extract_html_clock(x)
```

## Arguments

- x:

  character scalar

## Value

character scalar or `NA_character_`
