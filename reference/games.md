# Access all the games

`games()` scrapes all the games.

## Usage

``` r
games()
```

## Value

data.frame with one row per game

## Examples

``` r
# May take >5s, so skip.
all_games <- games()
```
