# Access all the expansion draft picks

`expansion_draft_picks()` returns historical expansion-draft picks with
one row per selected player/team/season combination.

## Usage

``` r
expansion_draft_picks()
```

## Value

data.frame with one row per pick

## Examples

``` r
all_expansion_draft_picks <- expansion_draft_picks()
```
