# Access all the players

`players()` scrapes all the players.

## Usage

``` r
players()
```

## Value

data.frame with one row per player

## Examples

``` r
# May take >5s, so skip.
all_players <- players()
```
