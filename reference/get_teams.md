# Access all the teams

`get_teams()` is deprecated. Use
[`teams()`](https://rentosaijo.github.io/nhlscraper/reference/teams.md)
instead.

## Usage

``` r
get_teams()
```

## Value

data.frame with one row per team
