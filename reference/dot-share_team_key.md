# Normalize share-plot team selector

`.share_team_key()` converts home/away shorthands into the canonical
team key used by share plots.

## Usage

``` r
.share_team_key(team)
```

## Arguments

- team:

  character scalar

## Value

character scalar, either `home` or `away`
