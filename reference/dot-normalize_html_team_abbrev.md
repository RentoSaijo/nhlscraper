# Normalize an HTML team abbreviation

`.normalize_html_team_abbrev()` uppercases and strips punctuation from
HTML team abbreviations so dotted legacy abbreviations still match.

## Usage

``` r
.normalize_html_team_abbrev(x)
```

## Arguments

- x:

  character vector of team abbreviations

## Value

normalized character vector
