# Access all the venues

`get_venues()` is deprecated. Use
[`venues()`](https://rentosaijo.github.io/nhlscraper/reference/venues.md)
instead.

## Usage

``` r
get_venues()
```

## Value

data.frame with one row per venue
