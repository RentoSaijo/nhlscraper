# Access all the countries

`get_countries()` is deprecated. Use
[`countries()`](https://rentosaijo.github.io/nhlscraper/reference/countries.md)
instead.

## Usage

``` r
get_countries()
```

## Value

data.frame with one row per country
