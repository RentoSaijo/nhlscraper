# Build an empty play-by-play delta context

`.empty_pbp_delta_context()` allocates an empty delta context sized to
the number of play-by-play rows.

## Usage

``` r
.empty_pbp_delta_context(n)
```

## Arguments

- n:

  integer row count

## Value

named list of empty delta vectors
