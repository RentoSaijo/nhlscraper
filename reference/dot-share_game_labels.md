# Read game labels for share plots

`.share_game_labels()` fetches the GameCenter summary and extracts the
team abbreviations and game date used in plot titles.

## Usage

``` r
.share_game_labels(game)
```

## Arguments

- game:

  game ID

## Value

named list of game labels
